const About = () => {
  return (
    <div style={center}>
      <div>About</div>
    </div>
  );
};

const center = {
  height: "50vh",
  width: "100%",
  display: "grid",
  placeItems: "center",
  fontSize: "3rem",
};

export default About;
